name: C/C++ CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      
    - name:  Build vendor1
      run: gcc -Wall -std=c11 -Isrc Icommon_obj/*.o ./vendor1/*.c -o opt
      
    - name: make
      run: make
      
    - name: make
      run: make check
      
    - name: make distcheck
      run: make distcheck
